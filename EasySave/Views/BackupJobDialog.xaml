<Window x:Class="EasySave.Views.BackupJobDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySave.Views"
        mc:Ignorable="d"
        Title="Backup Job"
        Height="420"
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F6F7F5"
        FontFamily="Segoe UI"
        FontSize="12">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryGreen" Color="#4C805B"/>
        <SolidColorBrush x:Key="PanelBorder" Color="#AFC5B5"/>
        <SolidColorBrush x:Key="PanelBg" Color="#F3F5F3"/>

        <Style TargetType="Border" x:Key="RoundedPanel">
            <Setter Property="Background" Value="{StaticResource PanelBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="FieldLabelStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox" x:Key="InputBoxStyle">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Background" Value="#D0DFD6"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="4,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="{StaticResource PrimaryGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryGreen}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="14"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="NeutralButtonStyle" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#D5D8D5"/>
            <Setter Property="BorderBrush" Value="#D5D8D5"/>
            <Setter Property="Foreground" Value="#2E2E2E"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Border Style="{StaticResource RoundedPanel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="90"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" Style="{StaticResource FieldLabelStyle}"/>
                <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding JobName}" Style="{StaticResource InputBoxStyle}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Source Path:" Style="{StaticResource FieldLabelStyle}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SourcePath}" Style="{StaticResource InputBoxStyle}" Margin="0,0,8,8"/>
                <Button Grid.Row="1" Grid.Column="2" Content="Browse" Click="BrowseSourceButton_Click" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Target Path:" Style="{StaticResource FieldLabelStyle}"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TargetPath}" Style="{StaticResource InputBoxStyle}" Margin="0,0,8,8"/>
                <Button Grid.Row="2" Grid.Column="2" Content="Browse" Click="BrowseTargetButton_Click" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Backup Type:" Style="{StaticResource FieldLabelStyle}"/>
                <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,2,0,8">
                    <RadioButton Content="Complete" IsChecked="{Binding IsCompleteType}" Margin="0,0,14,0"/>
                    <RadioButton Content="Differential" IsChecked="{Binding IsDifferentialType}"/>
                </StackPanel>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Description:" Style="{StaticResource FieldLabelStyle}" VerticalAlignment="Top"/>
                <TextBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                         Text="{Binding Description}"
                         Style="{StaticResource InputBoxStyle}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Height="74"
                         Margin="0,0,0,8"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3"
                           Text="{Binding ValidationMessage}"
                           Foreground="#D8473C"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Margin="0,2,0,8"/>

                <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Save" Click="SaveButton_Click" Width="88" Margin="0,0,8,0" Style="{StaticResource PrimaryButtonStyle}"/>
                    <Button Content="Cancel" Click="CancelButton_Click" Width="88" Style="{StaticResource NeutralButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
